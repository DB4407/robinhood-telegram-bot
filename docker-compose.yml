version: '3.8'

services:
  trading-bot:
    build: .
    container_name: autonomous-trading-bot
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "10000:10000"
    volumes:
      - ./data:/app/data
      - ./config:/app/config
